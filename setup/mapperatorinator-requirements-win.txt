# Windows install list for the Mapperatorinator INFERENCE env used by Beatex.
#
# Mirrors Mapperatorinator's own requirements.txt EXCEPT torchcodec is removed:
#   - torchcodec==0.10.0+cu130 ships no Windows wheel (Linux-only), and
#   - torchcodec is never imported by the inference path (audio decoding uses
#     pydub+ffmpeg; spectrograms use torchaudio.transforms).
# Install this into the Mapperatorinator venv AFTER torch + torchaudio (those come
# from the cu130 PyTorch index, not this file). See docs/model-setup.md.
accelerate==1.12.0
pydub==0.25.1
nnAudio==0.3.4
PyYAML==6.0.3
transformers==4.57.3
hydra-core==1.3.2
tensorboard==2.20.0
slider @ git+https://github.com/OliBomby/slider.git
torch_tb_profiler==0.4.3
wandb==0.24.2
matplotlib==3.10.8
pandas==2.3.3
pyarrow==22.0.0
einops==0.8.1
lightning==2.6.0
pywebview[qt]==6.1
pyqtwebengine==5.15.7
flask==3.1.2
audioop-lts; python_version>='3.13'
rich==14.2.0
peft==0.18.1
rosu-pp-py==3.1.0
datasets==4.8.3
huggingface-hub==0.36.2
hf-xet==1.4.2
